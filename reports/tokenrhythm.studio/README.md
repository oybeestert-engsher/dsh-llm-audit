# tokenrhythm.studio 端点审计报告

这里存放针对 `https://tokenrhythm.studio` 的高风险审计报告（Markdown + JSON）。
全量档逐模型审计 7 个对话模型（端点共 20 个），304 次调用 / 55 分钟。

## 说明

- 报告中的 Key 均为脱敏展示（`sk_tr_…297U`），不包含完整 API Key。
- 本地绝对路径已替换为 `~/.dsh/llm-audit/...`。
- 证据文件、危险 Key 台账等运行数据不随报告上传。
- 该报告是历史快照（2026-08-29，审计器 v0.4.2），仅供查阅；如需最新审计请重新运行插件。

## 结论

- 整体风险：高风险（63/100）
- 危险模型：4/7（deepseek-v4-flash-0731、deepseek-v4-pro-0813、glm-5.3、glm-5.3-flash）
- 主要问题：多模型对话历史被丢弃/改写（中转在动请求体）、system 被丢弃、
  后端轮换（同模型三连问自报不同身份）、`glm-5.3-flash` 流式路径输出被劫持
  （非流式正常——只篡改 stream=true 的响应）、全部模型扫盘（枚举/读取本地文件）、
  费用放大（接受 max_tokens=200000 不钳制）、隐藏提示可提取。
